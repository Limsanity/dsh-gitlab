# Dependencies
node_modules/

# Build output is COMMITTED on purpose: the adapt branch must be usable by
# `git clone` + `link:` on a machine that has no dsh dev workspace, where the
# build cannot run (host packages such as @deepseek-ai/cordis are not
# installable dependencies). Regenerate with `pnpm run build` when needed.

# Packed tarballs
*.tgz

# OS and tooling residue
.DS_Store
*.log
coverage/
*.tsbuildinfo
